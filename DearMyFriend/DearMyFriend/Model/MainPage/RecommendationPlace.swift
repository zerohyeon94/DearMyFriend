import UIKit

struct RecommendationPlace {
    var imageUrl: String?
    var pageUrl: String?
    var placeName: String?
}

struct PopularityModel {
    var imageUrl: String?
    var image: UIImage?
}
